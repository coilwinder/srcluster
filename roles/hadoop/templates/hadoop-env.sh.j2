#!/bin/bash
# NOTE: this files contains environment variables for all hadoop services
# NOTE: this files overwrites varibales from /etc/default/hadoop*
export HADOOP_LOG_DIR={{ hadoop_log_directory }}/hdfs
